import { solution } from "../../../src/ts/stack/10773";

describe("", () => {
  it("", () => {
    expect(solution([4, 3, 0, 4, 0])).toBe(0);
  });
  it("", () => {
    expect(solution([10, 1, 3, 5, 4, 0, 0, 7, 0, 0, 6])).toBe(7);
  });
});
